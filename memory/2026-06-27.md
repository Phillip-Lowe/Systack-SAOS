# 2026-06-27 11:59 CDT — Memory Search Config Restored

## What Happened
User reported memory search "needs to be fixed again." Investigation found `memorySearch` config in `~/.openclaw/openclaw.json` had been stripped to bare minimum (only `provider` and `model` fields remaining). All critical fields (`enabled`, `sources`, `hybrid`) were missing.

## Fix Applied
Restored full config via Python script:
```json
{
  "enabled": true,
  "sources": ["memory", "sessions"],
  "provider": "ollama",
  "model": "nomic-embed-text",
  "query": {
    "hybrid": {
      "enabled": true,
      "vectorWeight": 0.7,
      "textWeight": 0.3
    }
  }
}
```

## Verification
- `memory_search` returned 6 results with hybrid scoring ✅
- Backend: `builtin` (Ollama embedding) ✅
- Model: `nomic-embed-text` running (274MB) ✅

## Why It Keeps Breaking
Config stripped — likely by `openclaw doctor --fix`, manual edit, or partial config reload. Same pattern as 2026-06-13 incident.

## Files Changed
- `~/.openclaw/openclaw.json` — Restored `memorySearch` config

## Recommendations
- Consider adding config validation cron that alerts if `memorySearch.enabled` goes missing
- Document this as known pitfall — config drift is recurring

## Session Complete
Saved to MEMORY.md and daily log. No restart needed — config is live.
